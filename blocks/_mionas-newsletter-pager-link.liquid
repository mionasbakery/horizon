{%- doc -%}
  Renders a link to the older or the newer newsletter; its child blocks read that newsletter as closest.article.

  @param {blog} blog - The global blog of the newsletter being read.
{%- enddoc -%}
{%- liquid
  # blog.previous_article is the older email and blog.next_article the newer one (checked in the theme dev preview).
  assign is_newer = false
  if block.settings.direction == 'newer'
    assign is_newer = true
    assign target = blog.next_article
    assign fallback_label = 'mionas.newsletter.next' | t
  else
    assign target = blog.previous_article
    assign fallback_label = 'mionas.newsletter.previous' | t
  endif
  assign label = block.settings.label | default: fallback_label | escape
-%}
{%- comment -%} An empty cell keeps the other link on its own side. {%- endcomment -%}
{%- if target != blank -%}
  <a
    class="mionas-newsletter-pager-link{% if is_newer %} mionas-newsletter-pager-link--newer{% endif %}"
    href="{{ target.url }}"
    {{ block.shopify_attributes }}
  >
    {%- unless is_newer -%}
      <svg class="mionas-newsletter-pager-link__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 6l-6 6 6 6"/></svg>
    {%- endunless -%}
    <span class="mionas-newsletter-pager-link__text">
      {%- render 'mionas-text', content: label, variant: 'label', size: 'md', color: 'default', tag: 'span' -%}
      {%- content_for 'blocks', closest.article: target -%}
    </span>
    {%- if is_newer -%}
      <svg class="mionas-newsletter-pager-link__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 6l6 6-6 6"/></svg>
    {%- endif -%}
  </a>
{%- else -%}
  <span
    class="mionas-newsletter-pager-link{% if is_newer %} mionas-newsletter-pager-link--newer{% endif %}"
    {{ block.shopify_attributes }}
  ></span>
{%- endif -%}

{% stylesheet %}
  .mionas-newsletter-pager-link {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: calc(var(--space-md) * 2) var(--space-lg);
    color: var(--text-color-default);
    text-decoration: none;
  }

  .mionas-newsletter-pager-link--newer {
    justify-content: flex-end;
    text-align: end;
    border-inline-start: var(--list-base-border-width) solid var(--list-base-border-color);
  }

  .mionas-newsletter-pager-link__text {
    display: flex;
    flex-direction: column;
    gap: var(--space-2xs);
    min-width: 0;
    overflow-wrap: anywhere;
  }

  .mionas-newsletter-pager-link__chevron {
    flex: none;
    width: 24px;
    height: 24px;
    color: var(--brand-primary);
  }

  @media screen and (max-width: 749px) {
    .mionas-newsletter-pager-link {
      padding: var(--space-md);
    }

    .mionas-newsletter-pager-link__chevron {
      display: none;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Mionas: Pager link",
  "tag": null,
  "blocks": [
    {
      "type": "@theme"
    }
  ],
  "settings": [
    {
      "type": "select",
      "id": "direction",
      "label": "Links to",
      "options": [
        {
          "value": "older",
          "label": "Older newsletter"
        },
        {
          "value": "newer",
          "label": "Newer newsletter"
        }
      ],
      "default": "older"
    },
    {
      "type": "text",
      "id": "label",
      "label": "Label",
      "info": "Leave empty to use the theme's own wording, which stays translated per language."
    }
  ],
  "presets": [
    {
      "name": "Mionas: Pager link"
    }
  ]
}
{% endschema %}
